{
  "scripts": {
    "heroku-prebuild":"cd server && npm install && npm run development",
    "build": "cd server && yarn install && yarn development",
    "start": "cd server && yarn install && yarn development"
  }
}
